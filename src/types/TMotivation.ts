export type TMotivation = {
    id: number, 
    code: string, 
    description: string, 
    name: string, 
    transporterRequired: boolean,
    create_at_with_timezone: string,
    create_at_without_timezone:string
}